package gameElements.card;

public class MonsterCard extends Card{
    public MonsterCard(String id, CardType type, CardElement cardElement, int damage) {
        super(id, type, cardElement, damage);
    }
}
