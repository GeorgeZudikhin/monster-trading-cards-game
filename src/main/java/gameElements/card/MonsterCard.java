package gameElements.card;

public class MonsterCard extends Card{
    public MonsterCard(String id, CardType name, int damage, CardElement cardElement) {
        super(id, name, damage, cardElement);
    }
}
