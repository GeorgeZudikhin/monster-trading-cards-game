package gameElements.card;

public class SpellCard extends Card{
    public SpellCard(String id, CardType name, int damage, CardElement cardElement) {
        super(id, name, damage, cardElement);
    }
}
