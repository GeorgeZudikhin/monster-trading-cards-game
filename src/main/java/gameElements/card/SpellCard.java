package gameElements.card;

public class SpellCard extends Card{
    public SpellCard(String id, CardType type, CardElement cardElement, int damage) {
        super(id, type, cardElement, damage);
    }
}
